type VercelRequest={method?:string;body?:Record<string,string>|string;headers:Record<string,string|string[]|undefined>};type VercelResponse={status(code:number):VercelResponse;setHeader(name:string,value:string):void;send(body:string):void;json(body:unknown):void};
function clean(value:unknown,max=2000){return String(value??'').replace(/[\u0000-\u0008\u000B\u000C\u000E-\u001F]/g,'').trim().slice(0,max)}
export default async function handler(req:VercelRequest,res:VercelResponse){if(req.method!=='POST'){res.setHeader('Allow','POST');return res.status(405).send('Method not allowed')}const body=typeof req.body==='string'?Object.fromEntries(new URLSearchParams(req.body)):(req.body||{});const language=clean(body.language,2)==='de'?'de':'it';if(clean(body.website,200))return res.status(200).send('OK');const name=clean(body.name,120),phone=clean(body.phone,80),email=clean(body.email,160),service=clean(body.service,160),message=clean(body.message,3000),source=clean(body.source,120);if(!name||!phone||!service||!message)return res.status(400).send(language==='de'?'Pflichtfelder fehlen.':'Compila i campi obbligatori.');const apiKey=process.env.RESEND_API_KEY,to=process.env.QUOTE_TO_EMAIL||'info@hydrotechservice.ch',from=process.env.QUOTE_FROM_EMAIL;if(!apiKey||!from){console.error('Missing RESEND_API_KEY or QUOTE_FROM_EMAIL');return res.status(503).send(language==='de'?'Das Formular ist noch nicht konfiguriert. Bitte rufen Sie uns an oder schreiben Sie eine E-Mail.':'Il modulo non è ancora configurato. Chiamaci o scrivici via email.')}const text=[`Nome/Name: ${name}`,`Telefono/Telefon: ${phone}`,`Email: ${email||'-'}`,`Servizio/Leistung: ${service}`,`Source: ${source||'-'}`,'',message].join('\n');const response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${apiKey}`,'Content-Type':'application/json'},body:JSON.stringify({from,to:[to],reply_to:email||undefined,subject:`Richiesta Hydrotech — ${service}`,text})});if(!response.ok){console.error('Resend error',response.status,await response.text());return res.status(502).send(language==='de'?'Die Anfrage konnte nicht gesendet werden. Bitte versuchen Sie es später erneut.':'Non è stato possibile inviare la richiesta. Riprova più tardi.')}return res.status(200).send(language==='de'?'Vielen Dank. Ihre Anfrage wurde gesendet.':'Grazie. La tua richiesta è stata inviata.')}
